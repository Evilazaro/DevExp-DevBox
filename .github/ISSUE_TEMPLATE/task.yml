# .github/ISSUE_TEMPLATE/task.yml
name: ✅ Task
description: Concrete unit of work to implement part of a Feature
title: '✅ TASK — <Short Name>'
labels:
  - 'type:task'
assignees: []

body:
  - type: markdown
    attributes:
      value: |
        # TASK — <Short Name>
        A concrete unit of work. Keep scope tight and verifiable.

  - type: textarea
    id: task_description
    attributes:
      label: 📝 Task Description
      description: What exactly needs to be done?
      placeholder: >
        Add Bicep parameters for tags and implement consistent tagging across
        resources.
    validations:
      required: true

  - type: textarea
    id: deliverables
    attributes:
      label: 📦 Deliverables
      description: Concrete outputs of this task.
      placeholder: |
        - Updated Bicep module
        - Example parameters
        - Docs snippet
    validations:
      required: true

  - type: textarea
    id: steps
    attributes:
      label: 🔨 Implementation Steps
      description: Checklist-style steps (high-level, but actionable).
      placeholder: |
        - Update module parameters
        - Apply tags to all resources
        - Validate with `what-if`
        - Update documentation
    validations:
      required: false

  - type: textarea
    id: validation
    attributes:
      label: ✔️ Validation / Test Plan
      description: How will we verify it works?
      placeholder: |
        - Run `az deployment ... what-if`
        - Deploy to sandbox subscription
        - Confirm tags exist on all resources
    validations:
      required: true

  - type: textarea
    id: dependencies
    attributes:
      label: 🔗 Dependencies / Blockers
      description: Anything needed before this task can start/finish.
      placeholder: >
        Tag schema finalized; module structure agreed.
    validations:
      required: false

  - type: textarea
    id: definition_of_done
    attributes:
      label: 🎯 Definition of Done
      description: Criteria to close this task.
      placeholder: |
        - PR merged
        - Validation complete
        - Docs updated (if applicable)
    validations:
      required: true
