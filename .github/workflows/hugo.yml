# Sample workflow for building and deploying a Hugo site to GitHub Pages
name: Deploy Hugo site to Pages

on:
  workflow_dispatch:
  push:
    branches:
      - docs

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

defaults:
  run:
    shell: bash

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      HUGO_VERSION: "0.127.0"

    steps:
      - name: Update Packages
        run: |
          sudo apt-get update && sudo apt-get upgrade -y
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Setup Go 1.24.1
        uses: actions/setup-go@v5
        with:
          # Semantic version range syntax or exact version of Go
          go-version: '1.24.1'
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "22"

      - name: Install npm dependencies
        run: |
          echo "Checking for package-lock.json..."
          if [ -f package-lock.json ]; then
            npm ci
          else
            echo "No package-lock.json found, installing dependencies..."
            npm install --save-exact @fortawesome/fontawesome-free@latest bootstrap@latest
            npm install --save-exact -D hugo-extended@${HUGO_VERSION}
            npx npm-check-updates -u && npm run cd:docs update:pkgs
            # npm install --save-dev postcss postcss-cli autoprefixer
            # npm install --save-dev postcss-rtlcss rtlcss
            echo "Dependencies installed."
          fi

      - name: Install Hugo CLI
        run: |
          wget -qO hugo.deb \
            https://github.com/gohugoio/hugo/releases/download/v${HUGO_VERSION}/hugo_extended_${HUGO_VERSION}_linux-amd64.deb
          sudo dpkg -i hugo.deb

      - name: Install Dart Sass
        run: sudo snap install dart-sass --classic

      - name: Configure Pages
        id: pages
        uses: actions/configure-pages@v5

      - name: Build site with Hugo
        env:
          HUGO_CACHEDIR: ${{ runner.temp }}/hugo_cache
          HUGO_ENVIRONMENT: production
        run: |
          hugo \
            --minify \
            --baseURL "${{ steps.pages.outputs.base_url }}/"

      - name: Upload artifact for Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: public

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
